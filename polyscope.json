{
  "scripts": {
    "setup": "npm install && npm run build && cd build && herd link \"$(basename \"$(dirname \"$PWD\")\")\"",
    "archive": "herd unlink"
  },
  "preview": {
    "url": "http://{{folder}}.test"
  }
}
