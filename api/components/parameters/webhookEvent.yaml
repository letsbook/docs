type: array
minItems: 1
items:
  type: string
  enum:
    - booking.drafted
    - booking.confirmed
    - booking.completed
    - booking.canceled
    - booking.expired
    - order.canceled
description: Events to get notified about
example:
  - booking.confirmed
  - booking.canceled
