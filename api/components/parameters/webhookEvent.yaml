type: array
minItems: 1
items:
  type: string
  enum:
    - booking.drafted
    - booking.confirmed
    - booking.completed
    - booking.canceled
    - booking.expired
    - order.confirmed
    - order.lines_updated
    - order.canceled
    - customer.anonymized
    - customer.registered
    - customer.updated
description: Events to get notified about
example:
  - booking.confirmed
  - booking.canceled
